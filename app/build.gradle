apply plugin: 'com.android.application'

android {
    compileSdkVersion 26
    buildToolsVersion '26.0.2'

    signingConfigs {
        calculator_release {
            keyAlias "zhuchengwei"
            keyPassword "zcw699390zhg"
            storePassword "zcw699390zhg"
            storeFile file("$rootDir/zhuchengwei.jks")
        }
    }

    defaultConfig {
        applicationId "com.zcw.calculator"
        minSdkVersion 16
        targetSdkVersion 19
        versionCode 1
        versionName "1.0.1"

        signingConfig signingConfigs.calculator_release
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    productFlavors {
    }
}

dependencies {
    testCompile 'junit:junit:4.12'

    compile fileTree(dir: 'libs', include: ['*.jar'])

    compile "com.android.support:appcompat-v7:26.0.2"
}
